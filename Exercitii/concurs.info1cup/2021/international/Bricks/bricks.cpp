#include "bricks.h"

int solve(int N, bool C[], int H[]) {
    int result;

    // se citesc datele
    std::cin >> N;
    for (int i = 0; i < N; i++) {
        std::cin >> C[i];
    }
    for (int i = 0; i < N; i++) {
        std::cin >> H[i];
    }

    

    return result;
}